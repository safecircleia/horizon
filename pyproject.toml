[build-system]
requires = ["setuptools>=68.0", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["tui*"]

[project]
name = "horizon"
version = "0.1.0"
description = "SafeCircle Risk Detection Model Training System"
readme = "README.md"
requires-python = ">=3.10"
license = "LicenseRef-TBD"
authors = [
    {name = "SafeCircle Team", email = "team@safecircle.tech"}
]
keywords = ["ai", "safety", "child-protection", "nlp", "llm"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
]

dependencies = [
    "torch>=2.11.0",
    "transformers>=5.7.0",
    "peft>=0.19.0",
    "bitsandbytes>=0.49.0",
    "accelerate>=1.13.0",
    "datasets>=4.8.0",
    "huggingface_hub>=1.13.0",
    "anthropic>=1.4.0",
    "openai>=3.8.0",
    "scikit-learn>=1.8.0",
    "pandas>=2.2.0",
    "numpy>=1.26.0",
    "pydantic>=2.13.0",
    "pyyaml>=6.0.0",
    "tqdm>=4.66.0",
    "rich>=15.0.0",
    "typer>=0.25.0",
    "importlib-metadata>=6.0.0",
    "trl>=1.4.0",
    "textual>=8.2.0",
]

[project.optional-dependencies]
dev = [
    "black>=24.4.0",
    "ruff>=0.4.0",
    "pytest>=8.2.0",
    "pytest-asyncio>=0.23.0",
    "pytest-cov>=5.0.0",
    "pre-commit>=3.7.0",
    "jupyter>=1.0.0",
]
export = [
    "onnxruntime>=1.19.0",
    "onnx>=1.16.0",
    "onnxscript>=0.1.0",
]
api = [
    "fastapi>=0.136.0",
    "uvicorn>=0.46.0",
    "vllm>=0.4.0",
]
tracking = [
    "wandb>=0.16.0",
    "tensorboard>=2.16.0",
]
viz = [
    "matplotlib>=3.8.0",
    "seaborn>=0.13.0",
    "ipywidgets>=8.1.0",
]

[project.scripts]
horizon = "tui.app:main"
horizon-train = "training.scripts.train:main"
horizon-eval = "evaluation.metrics.evaluate:main"
horizon-generate = "data.generation.generators.main:cli"
horizon-serve = "inference.api.serve:main"

[tool.black]
line-length = 88
target-version = ['py310', 'py311', 'py312']
include = '\.pyi?$'
extend-exclude = '''
/(
  # directories
  \.eggs
  | \.git
  | \.hg
  | \.mypy_cache
  | \.tox
  | \.venv
  | build
  | dist
)/
| notebooks/horizon_training\.py
'''

[tool.ruff]
line-length = 88
target-version = "py310"
extend-exclude = ["notebooks/horizon_training.py", "notebooks/horizon_training.ipynb"]

[tool.ruff.lint]
select = [
    "E",  # pycodestyle errors
    "W",  # pycodestyle warnings
    "F",  # pyflakes
    "I",  # isort
    "B",  # flake8-bugbear
    "C4", # flake8-comprehensions
    "UP", # pyupgrade
]
ignore = [
    "E501",  # line too long (handled by black)
    "B008",  # do not perform function calls in argument defaults
    "C901",  # too complex
]

[tool.ruff.lint.per-file-ignores]
"__init__.py" = ["F401"]
"tests/**/*.py" = ["E402"]
"training/scripts/train.py" = ["E402"]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = "test_*.py"
python_classes = "Test*"
python_functions = "test_*"
addopts = """
    --verbose
    --cov=.
    --cov-report=term-missing
    --cov-report=html
"""

[tool.coverage.run]
source = ["."]
omit = [
    "*/tests/*",
    "*/experiments/*",
    "*/notebooks/*",
    "*/__pycache__/*",
]

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "def __repr__",
    "raise AssertionError",
    "raise NotImplementedError",
    "if __name__ == .__main__.:",
    "if TYPE_CHECKING:",
]
