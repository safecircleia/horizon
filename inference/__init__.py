"""Inference runtime for SafeCircle Horizon on-device models.

Submodules:
    adaptive  — model tier selection and execution policy
    memory    — lifecycle management, lazy load/unload, RSS monitoring
"""
